import { FireIcon as HeroFireIcon } from '@heroicons/react/20/solid';

const FlameIcon = ({ className }: { className?: string }) => {
   return <HeroFireIcon className={className} />;
};

export default FlameIcon;
