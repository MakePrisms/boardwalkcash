import { ChartBarIcon as HeroChartIcon } from '@heroicons/react/20/solid';

const ChartIcon = ({ className }: { className?: string }) => {
   return <HeroChartIcon className={className} />;
};

export default ChartIcon;
