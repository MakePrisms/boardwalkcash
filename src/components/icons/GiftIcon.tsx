import { GiftIcon as HeroGiftIcon } from '@heroicons/react/20/solid';

const GiftIcon = ({ className }: { className?: string }) => {
   return <HeroGiftIcon className={className} />;
};

export default GiftIcon;
